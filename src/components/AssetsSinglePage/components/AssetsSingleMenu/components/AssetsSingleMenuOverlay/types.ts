import { EAssetsSingleMenuStatesKeys } from "../../types";

export type AssetsSingleMenuOverlayProps = {
  setVisibleFalse: (value: EAssetsSingleMenuStatesKeys) => void
  setVisibleTrue: (value: EAssetsSingleMenuStatesKeys) => void
}
